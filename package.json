{
    "name": "seating-chart",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler pages dev --port 8787",
        "deploy": "wrangler pages deploy"
    },
    "devDependencies": {
        "wrangler": "^4.100.0"
    }
}
